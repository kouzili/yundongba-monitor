# 给 agent 的安装说明

这个仓库提供一个查询上海「韵动吧」订场平台的命令行工具。你要做两件事：装好它，
然后按**你自己框架的约定**把它注册成可调用的能力。

## 1. 装

```bash
bash install.sh
```

会建虚拟环境、装依赖、跑 217 个自检、检查凭据、打一个真实请求确认签名有效。

**预期会失败一次**：仓库里不含密钥（`config.json` 在 `.gitignore` 里），所以
`install.sh` 会在「凭据」那步报缺 `secret_api` 并退出。这是设计如此 —— 没有它
任何查询都只会返回「签名错误」，装完也没用。

这时**告诉用户去补凭据，不要向用户索要密钥内容**：

```bash
./ydb credentials      # 交互式，getpass 不回显，不进 shell history
```

密码属于用户的真实账号凭据，**绝不能出现在对话里**。用户填完后重跑
`bash install.sh` 确认冒烟测试通过。

只填 `secret_api`（+ 查畅打则加 `secret_ydb`）就能用全部查询能力；
`mobile` / `password` 只有需要自动锁单时才要。

## 2. 注册

功能入口是仓库根目录的 `./ydb`，一个普通命令行程序：

```bash
./ydb search 徐泾
./ydb slots 1189 --date 2026-08-24 --from 18 --to 21 --json
```

所有子命令输出统一信封 `{"ok": bool, "summary": "一句话结论", "data": {...}}`。
简单问题转述 `summary`，需要挑时段/比价/排序时用 `data` 自己算。

**它不依赖任何 skill 机制** —— 能跑 shell 就能用。

能力说明书在 `skill/SKILL.md`（含 `__YDB_HOME__` 占位符，会被替换成本机绝对路径）。
按你的框架怎么发现工具来选：

| 你的情况 | 做法 |
|---------|------|
| 读 `~/.claude/skills/`（Claude Code 约定） | `bash install.sh`，默认就装这儿 |
| 读别的目录 | `bash install.sh --skill-dir <你的目录>` |
| 需要把描述塞进系统提示词或项目文档 | `bash install.sh --print-skill` 取内容 |
| 走 MCP | `SKILL.md` 用不上，需要另写一层 MCP server 包 `cli.py`（尚未实现） |

装完请告诉用户你把说明书放到了哪里、以及为什么选那个位置。

## 必须转述给用户的约束

- **`./ydb book` 会在用户账号上产生真实订单**，没有 `--yes` 会被拒绝。除非用户明确
  要求下单，不要调用。
- **同一时间只允许一笔未处理的场地订单**，付款窗口约 11 分钟，**过期不会自动清除**，
  必须显式 `./ydb cancel <orderid>` 或去 App 付款，否则一直挡住后续所有锁单。
- **`./ydb watch` 是长跑命令**，会一直占着进程直到轮数用完、锁到单或被中断。
  当后台任务调度，不要当一次性查询等它返回。
- 所有请求经全局配速器（默认 10 次/分钟，状态跨进程共享），所以查多个场馆 × 多天
  会明显变慢。按需要的最小范围查。

更多平台边界（仅上海、各馆放场天数不同、`status=4` 的含义）见 `skill/SKILL.md`。
